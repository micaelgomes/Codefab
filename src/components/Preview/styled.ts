import styled from "styled-components";

export const Container = styled.div`
  position: fixed;
  background: #fff;
  width: 30%;
  height: 50%;
  cursor: move;

  padding: 1rem;
  border-radius: 5px;

  z-index: 9999999;
  top: 100px;
  right: 16px;
  margin: auto;
  user-select: none;
`;
